import { Request, Response } from 'express';
import Publis, { publis as Publi } from '../Model/PubliModel';

//Função para listar as publicaçôes...
export const list = async (req: Request, res: Response): Promise<void> => {
    
}